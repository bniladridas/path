repos:
- repo: https://github.com/astral-sh/ruff-pre-commit
  rev: v0.1.13  # Use the latest version
  hooks:
    - id: ruff
      args: [--fix, --exit-non-zero-on-fix]
    - id: ruff-format
      args: [--line-length=127]  # Match your pyproject.toml config
